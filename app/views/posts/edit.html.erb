<%= form_for @post do |form| %>
  <%= form.label :message %>
  <%= form.hidden_field :user_id, :value => @post.user_id %>
  <%= form.text_area :message %>
  <div class="waves-effect waves-light btn-large"><%= form.submit "Submit" %></div>
<% end %>
